# 沉浸式剧本杀场馆实景道具剧本主题绑定人物角色溯源系统

## 项目名称
沉浸式剧本杀场馆实景道具剧本主题绑定人物角色溯源系统

## 项目路径
/Users/Admin/Desktop/solo-0601/qd-0601/qd-组2/qd-138

## 访问地址
- 前端地址: http://localhost:8138
- 后端API: http://localhost:8148/api

## 端口配置

| 服务 | 端口 | 绑定地址 |
|------|------|----------|
| 前端 | 8138 | 127.0.0.1 |
| 后端 | 8148 | 127.0.0.1 |
| MySQL | 3364 | 127.0.0.1 |
| Redis | 6437 | 127.0.0.1 |

## 启动命令

```bash
# 构建并启动所有服务
docker compose up -d --build

# 仅启动（使用已构建镜像）
docker compose up -d

# 查看日志
docker compose logs -f

# 停止服务
docker compose down

# 使用启动脚本
chmod +x start.sh
./start.sh
```

## 验证结果

### 后端单独编译
- 状态: ✅ 已通过
- 命令: `docker run --rm -v $(pwd)/backend:/app -w /app maven:3.9-eclipse-temurin-17 mvn compile -q`
- 说明: 在Docker容器内使用maven:3.9-eclipse-temurin-17镜像编译通过，无错误输出

### 前端单独构建
- 状态: ✅ 已通过
- 命令: `npm install --registry=https://registry.npmmirror.com && npm run build`
- 说明: 本地Node.js 26.0.0环境构建成功，已生成package-lock.json

### Docker构建启动
- 状态: ✅ 已通过
- 命令: `docker compose up -d --build`
- 容器状态: 4个容器全部正常运行
- 依赖缓存: 后端Maven依赖和前端npm依赖均启用Docker层缓存

### localhost与127.0.0.1验证
- localhost: http://localhost:8138 → Status: 200 ✅
- 127.0.0.1: http://127.0.0.1:8138 → Status: 200 ✅
- 内容对比: 完全一致 ✅

### 浏览器页面验证
- 状态: ✅ 已通过
- 页面标题: 剧本杀道具溯源系统
- 菜单内容: 剧本主题管理、人物角色管理、道具档案管理、角色道具关联、双向溯源查询、变更记录
- 中文显示: 正常 ✅

### 中文显示验证
- 状态: ✅ 已通过
- 验证方式: `curl http://localhost:8148/api/theme`
- 结果: 中文内容正常显示，无乱码
- 修复措施:
  - SQL初始化脚本添加 `SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci`
  - MySQL连接串添加 `characterEncoding=utf8`
  - 后端添加 WebConfig 配置 UTF-8 消息转换器
  - Dockerfile 设置 `JAVA_TOOL_OPTIONS="-Dfile.encoding=UTF-8"`
  - 删除旧MySQL数据卷，重新初始化

## 技术架构

- **前端**: Vue3 + Vite + Element Plus + TypeScript
- **后端**: Spring Boot 3.3 + JDK 17 + JPA
- **数据库**: MySQL 8.0
- **缓存**: Redis 7
- **容器**: Docker + Docker Compose

## 核心功能

1. **实景剧本道具基础档案录入** - 编号、适配时代、道具类型管理
2. **剧本主题、人物角色双重绑定登记** - 三层关联架构
3. **剧本角色道具更换** - 自动更新关联关系并留存变更记录
4. **双向溯源查询** - 按剧本/角色查询配套全部道具
5. **道具编号扫码建档** - 摄像头/扫码枪扫描条码，解析码值后预填编号建档，扫不到可手动录入；溯源页支持按道具编号直查当前人物

## 项目结构

```
qd-138/
├── backend/              # Spring Boot后端
│   ├── src/main/java/com/example/scriptkill/
│   │   ├── controller/    # REST控制器
│   │   ├── service/       # 业务逻辑
│   │   ├── repository/    # 数据访问层
│   │   ├── entity/        # 实体类
│   │   ├── dto/           # 数据传输对象
│   │   └── config/        # 配置类
│   ├── src/main/resources/
│   │   ├── db/init.sql    # 数据库初始化脚本
│   │   └── application*.yml # 应用配置
│   └── Dockerfile         # 后端Dockerfile（含DOCKER_REGISTRY变量）
├── frontend/             # Vue3前端
│   ├── src/
│   │   ├── views/         # 页面组件
│   │   ├── api/           # API接口定义
│   │   └── router/        # 路由配置
│   ├── Dockerfile         # 前端Dockerfile（含DOCKER_REGISTRY变量）
│   └── package-lock.json  # npm依赖锁定文件
├── .env                  # 环境变量配置
├── docker-compose.yml     # Docker Compose配置（已删除version行）
├── .gitignore            # Git忽略文件
├── .dockerignore         # Docker忽略文件
├── README.md             # 项目说明文档
├── start.sh              # 启动脚本（已chmod +x）
└── qd-138.md             # 项目交付文档
```

## 开发记录

### Docker构建优化
- 前端Dockerfile采用多阶段构建，先COPY package.json安装依赖，再COPY源码build
- 后端Dockerfile采用多阶段构建，先COPY pom.xml下载依赖，再COPY src编译
- 所有基础镜像统一使用 `ARG DOCKER_REGISTRY` 变量，支持自定义镜像仓库
- 使用京东npm国内镜像加速前端依赖安装
- 使用华为云Maven镜像加速后端依赖下载

### 中文乱码修复
- 所有配置文件确保UTF-8编码
- MySQL建库建表使用utf8mb4字符集
- 后端响应设置UTF-8编码
- 删除旧数据卷避免遗留乱码数据

### 端口管理
- 所有端口固定配置在.env文件中
- Docker Compose端口绑定127.0.0.1
- 启动脚本包含端口占用检测和释放功能

### 道具扫码建档与编号溯源（2026-09-14）
- 后端新增 `POST /api/prop/scan-parse`：归一化条码原始内容（去控制字符、AIM标识符、URL/键值包装），返回解析编号及是否已建档
- 后端新增 `GET /api/role-prop/prop-code/{propCode}`：按道具编号直查当前绑定人物
- 建档接口增加编号重复校验，防止扫码重复建档
- 前端道具档案页新增「扫码建档」：html5-qrcode 摄像头识别 + 扫码枪/手动录入兜底，解析成功预填编号进入建档表单
- 前端溯源页「按道具查询」支持扫码/输入编号查询，结果页置顶展示当前人物

### 人物回看（2026-09-15）
- 后端新增 `GET /api/change-record/character-review?date=yyyy-MM-dd`（日期缺省为当天）：按人物和日期汇总当天道具变更，回看名单与当天明细同源统计、一次返回，刷新后两侧保持一致
- 未填写变更原因的记录标记为「未确认变更」，名单与明细均统计/标出未确认数量
- 角色已删除的历史记录归入「未知人物」分组，不丢失核对线索
- 前端新增「人物回看」页（/review）：日期选择 + 当天统计（变更总数/涉及人物/未确认变更）+ 回看名单（按人物汇总绑定/解绑/更换次数，可点选人物过滤明细）+ 当天明细

### 修复清单
| 序号 | 问题 | 修复方式 | 状态 |
|------|------|----------|------|
| 1 | 后端编译未在Docker容器内验证 | 使用maven:3.9-eclipse-temurin-17容器执行mvn compile -q | ✅ |
| 2 | Dockerfile未使用DOCKER_REGISTRY变量 | 添加ARG DOCKER_REGISTRY并在FROM中引用 | ✅ |
| 3 | docker-compose.yml含obsolete version行 | 删除version: '3.8' | ✅ |
| 4 | 前端缺少package-lock.json | 在frontend目录执行npm install生成 | ✅ |
| 5 | start.sh未添加可执行权限 | 执行chmod +x start.sh | ✅ |
| 6 | 浏览器验证未执行 | 使用浏览器访问http://localhost:8138验证页面正常 | ✅ |

## 默认前端访问地址

http://localhost:8138
